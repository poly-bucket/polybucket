export const routes = {
    default: "/",
    dashboard: "/dashboard",
    login: "/login",
    modelDetail: "/model/:id",
    profile: '/profile',
    userProfile: '/profile/:userId',
    settings: '/settings',
    plugins: '/admin/plugins',
    reports: '/admin/reports',
} as const;